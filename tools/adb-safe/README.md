# Đã rút gọn → `Fix-Xanh-Man/`

Tool menu cũ đã bỏ. Dùng gói slim:

- Thư mục: [`../Fix-Xanh-Man/`](../../Fix-Xanh-Man/)
- Double-click **`Fix.bat`**
- Home Windows: `D:\TOOL\Fix Xanh Màn Khởi Động lại`

`AdbSafe.bat` ở đây chỉ gọi `Fix-Xanh-Man\Fix.ps1` (tương thích tên cũ).
